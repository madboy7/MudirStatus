# نظام حالة المدير (MudirStatus)

هذا هو الكود المصدري لتطبيق إدارة حالة المدير (موبايل + آيباد).

## 🛠️ المتطلبات المسبقة (قبل البدء)
لكي يعمل هذا المشروع على جهازك وبناء التطبيق للنشر، يجب تثبيت **Node.js**:
1. حمل البرنامج من الرابط الرسمي: https://nodejs.org/
2. اختر نسخة **LTS** (النسخة المستقرة).
3. قم بتثبيت البرنامج.
4. **مهم:** بعد التثبيت، أغلق سطر الأوامر (Terminal) وافتحه من جديد.

---

## 🚀 خطوات التشغيل

### 1. تثبيت المكتبات البرمجية
افتح المجلد في التيرمنال واكتب:
```bash
npm install
```

### 2. إعداد مفتاح الذكاء الاصطناعي (Gemini)
1. أنشئ ملفاً جديداً في المجلد الرئيسي وسمّه `.env`
2. افتح الملف بالمفكرة واكتب بداخله السطر التالي (استبدل X بمفتاحك):
```
VITE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXX
```

### 3. تجربة التطبيق على جهازك
لتشغيل التطبيق محلياً للتأكد من عمله:
```bash
npm run dev
```
(اضغط على الرابط الذي سيظهر لك في التيرمنال لفتح التطبيق).

---

## 🌐 خطوات الرفع والنشر (Firebase)

لجعل التطبيق يعمل أونلاين ولحل مشاكل الآيفون:

1. **تثبيت أدوات Firebase (مرة واحدة فقط):**
   ```bash
   npm install -g firebase-tools
   ```

2. **تسجيل الدخول لحسابك:**
   ```bash
   firebase login
   ```

3. **بناء النسخة النهائية:**
   هذا الأمر سيقوم بضغط الملفات وتجهيزها في مجلد `dist`:
   ```bash
   npm run build
   ```

4. **رفع الملفات:**
   ```bash
   firebase deploy
   ```

بعد الانتهاء، سيعطيك رابطاً (Hosting URL). هذا هو الرابط الذي ستستخدمه على الموبايل والآيباد.
